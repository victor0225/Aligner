# Subjector 사용법

Subjector는 IDEA CRUISE에서 승인된 task를 받아 Slack에서 실행, 제출, 변경, 누적 기록을 관리하는 도구다. 회의 중 task를 새로 해석하는 곳이 아니라, 담당자가 지금 바로 착수할 수 있게 만드는 작업 실행판이다.

## 사용하는 곳

- `#in-process`: 개인별 task 보드가 올라오는 채널
- `#회의-결과록`: IDEA CRUISE 회의 결과록과 보조 회의 전사 맥락을 남기는 채널
- `#finals`: 프로젝트 현재 상태, 변경 이력, 다음에 볼 것을 누적 정리하는 채널
- Subjector Bot DM: `출근`, `최신화`, `퇴근`, `회의 기록: 내용`, `finals 업데이트` 명령을 보내는 곳

## 하루 시작

1. Subjector Bot DM에 `출근`을 보낸다.
2. `#in-process`에서 내 task 보드를 확인한다.
3. 할 task에서 `수락`을 누른다.
4. 수락 뒤에는 `도움 받기`, `작업 제출`, `변경` 버튼이 보인다.

`최신화`를 보내면 출근 기록은 건드리지 않고 `#in-process` 보드만 다시 갱신한다.

## task 카드 읽는 법

task는 IDEA CRUISE에서 넘어온 실행 항목이다. 핵심은 세 가지다.

- 해야 할 일: 담당자가 실제로 만들어야 하는 산출물
- 확인할 것: 작업 중 반드시 확인해야 하는 정보
- 완료 기준: 제출물이 task를 끝냈다고 볼 수 있는 기준

Subjector는 담당자, 중요도, 상태를 보여주지만, 작업의 의미를 다시 늘려 해석하지 않는다. 의미와 기준은 IDEA CRUISE에서 정하고, Subjector는 실행 흐름을 관리한다.

## 수락과 도움 받기

`수락`을 누르면 task 상태가 수락으로 바뀐다. Codex를 꼭 쓰지 않아도 된다.

`도움 받기`는 담당자가 자신의 배경지식과 진행 의도를 입력하면, 이 task에서 사람이 직접 해야 할 일과 Codex에게 맡기면 좋은 일을 나눠 안내한다. 예를 들어 자료조사 task라면 담당자는 판단 기준을 정하고, Codex는 비교표 초안, 검색 키워드, 문서 정리 틀을 도울 수 있다.

## 작업 제출

`작업 제출`을 누르면 제출 모달이 열린다.

- 제출 내용: 무엇을 했고 어떤 결과가 나왔는지 적는다.
- 첨부 파일: Word, PDF, 이미지, 표, 조사 자료 등을 올린다.

제출 뒤 Subjector는 완료 기준을 기준으로 결과를 평가한다. 완료 기준을 충족하면 완료로 정리되고, 부족하면 추가 진행 예정 상태로 남는다. 추가 진행 예정 task는 바로 다시 띄우지 않고 다음 날 출근 시 갱신된 task로 이어서 확인한다.

## 변경

작업 중 task를 현실에 맞게 바꿔야 하면 `변경`을 누른다.

변경 모달에는 세 영역이 있다.

- 해야 할 일 변경
- 확인할 것 추가/수정
- 완료 기준 변경 제안

완료 기준을 바꾸면 영향이 크기 때문에 변경 전후를 확인하는 경고가 나온다. 담당자가 확인하면 기존 `#in-process` 메시지가 업데이트되고, 변경 이력은 `#finals`의 `충돌/변경 이력`에 반영될 수 있다.

## 회의 맥락 보강

IDEA CRUISE를 회의 중 사용하지 못했거나 녹음본을 AI가 참고하면 좋겠다면 `#회의-결과록`에 녹음 파일을 올린다. Subjector는 전사 요약을 저장하고, 자동 task 생성은 하지 않는다. 저장된 맥락은 이후 IDEA CRUISE 카드 생성에 참고된다.

짧은 기록은 DM으로 남길 수 있다.

```text
회의 기록: 오늘은 라즈베리파이보다 ESP32 기준으로 비교하기로 함
```

이 기록도 IDEA CRUISE 참고 맥락으로 저장되며 자동 task 생성은 하지 않는다.

## 퇴근과 finals

퇴근 전 Subjector Bot DM에 `퇴근`을 보낸다. Subjector는 그날 완료, 추가 진행 예정, 변경 요청, 미정리 task를 요약한다.

`#finals`는 세 영역으로 누적 정리된다.

- 프로젝트 현재 상태: 오늘 회의/작업 이후 프로젝트가 어디까지 왔는지
- 충돌/변경 이력: task 변경, 기준 변경, 기존 방향과 충돌한 내용
- 다음 회의/내일 볼 것: 다음에 결정하거나 확인해야 할 기준

수현님은 필요할 때 DM에 `finals 업데이트`를 보내 즉시 최신 요약을 만들 수 있다.

## 자주 쓰는 명령

```text
출근
최신화
퇴근
회의 기록: 내용
finals 업데이트
테스트 task 정리
```

`테스트 task 정리`는 수현님 전용 테스트 정리 명령이다. 제목에 `테스트`가 들어간 미완료 task를 보관 처리하고 `#in-process`를 갱신한다.

